version: '3.8'

services:
  trivia-web:
    build: .
    image: trivia-game:latest
    ports:
      - "8080:80"
    restart: unless-stopped
    volumes:
      - .:/usr/share/nginx/html:ro
    # allows live editing of files in the container if desired
